# Frontend Environment Variables
# Copy this to .env.local for local development

# === BACKEND CONNECTION ===

# Local development (when running backend on localhost)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# Production (uncomment and use when deploying to Vercel)
# NEXT_PUBLIC_API_URL=https://artisan-rem1.onrender.com
# NEXT_PUBLIC_WS_URL=wss://artisan-rem1.onrender.com/ws

# === NOTES ===
# 1. For Vercel deployment, set these in Vercel Dashboard → Settings → Environment Variables
# 2. Don't commit .env.local to git
# 3. After changing env vars, restart the dev server
